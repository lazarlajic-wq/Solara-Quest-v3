{
  "name": "solara-quest",
  "private": true,
  "version": "0.1.0",
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "scripts": {
    "dev": "npm run dev --workspace=apps/client",
    "build": "npm run build --workspace=apps/client",
    "typecheck": "npm run typecheck --workspace=apps/client",
    "validate:assets": "node tools/asset-validation/validate-assets.mjs"
  },
  "devDependencies": {
    "typescript": "^5.6.3"
  }
}
